import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.circuitjam.lite',
  appName: 'Circuit Jam Lite',
  webDir: 'dist/public'
};

export default config;
